import React, { useState } from 'react';

import './App.css';

function App() {
  const [newNote,setNewNote] = useState('')
  const [notes,setNotes] = useState([]);

  return (
    <div className="App">
     
    </div>
  );
}

export default App;
